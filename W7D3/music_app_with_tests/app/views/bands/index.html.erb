<h1>All your bands</h1>

<% @bands.each do |band| %>
  <h3> 
    <form action = "<%= band_url(band) %>" method = "get">
    <input type = "submit" value = "<%= band.name %>">
  </h3>
  <br>
<% end %>

<br>

<form action = "<%= new_band_url %>" method = "get">
<input type = "submit" value = "Add a band to collection">